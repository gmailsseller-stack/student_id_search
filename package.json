{
  "name": "student-search-web",
  "version": "1.0.0",
  "description": "Web page for searching student passwords",
  "scripts": {
     "start": "npx serve . -l $PORT"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}
